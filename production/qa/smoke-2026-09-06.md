## Smoke Check Report
**Date**: 2026-09-06
**Sprint**: Not identified (no sprint plan found in `production/`)
**Engine**: Godot 4.7 (Godot 4.7.1 mono binary used for execution)
**QA Plan**: Not found — run /qa-plan first
**Argument**: sprint (default; no args given, no `--platform` flag)

---

### Automated Tests

**Status**: PASS via safety net (57 tests, 57 passing) · gdUnit4 suite NOT RUN (addon not installed)

- `tests/standalone_check.gd` executed with
  `Godot_v4.7.1-stable_mono_win64_console.exe --headless --script tests/standalone_check.gd`
  → **57 pasan, 0 fallan** (exit 0; only benign ObjectDB/resource-leak warnings at exit):
  - Fórmula 1 escalones calidad_timing/postura_daño (D1, D14) — 16/16
  - Parry Justo predicado y bono + R8 (C25) — 5/5
  - Fórmula 8 daño golpe enemigo, vida_maxima, supervivencia (D11, D12) — 5/5
  - R10a presupuesto reliquias (D15) — 5/5
  - Clamps recursos agotables (E12, E13) — 4/4
  - Invariantes R1–R8 + cobertura ≈59% (D9a) — 10/10
  - Borde de Castigo (C11) — 2/2
  - Ciclo de parry contra stub (C6, C12a, C19, C20, C21, C24) — 10/10
- gdUnit4 suite NOT RUN: `addons/gdUnit4/` is not installed in this checkout, so the
  runner `res://addons/gdUnit4/bin/GdUnitCmdTool.gd` is unavailable (per `tests/README.md`,
  the `tests/gdunit4_runner.gd` path from the old skill text never existed). Test files
  declare last green run of 28/28 (`combate_formulas_test.gd`) against gdUnit4 6.2.0 on
  2026-08-05.

"Manual confirmation required: did tests pass in your local IDE or CI? This
will determine whether the automated test row contributes to a FAIL verdict."
(Unconfirmed NOT RUN is treated as PASS WITH WARNINGS, not FAIL.)

---

### Test Coverage

| Story | Type | Test File | Coverage Status |
|-------|------|-----------|----------------|
| (no sprint/epic stories found — nothing to map) | — | — | UNKNOWN |

**Summary**: 0 covered, 0 manual, 0 missing, 0 expected, 1 unknown (no story list).

Existing test inventory (no stories to attach to, recorded for `/story-done` follow-up):
- `tests/unit/combate/combate_formulas_test.gd` — fórmulas (D1–D3, D6–D9a, D11, D12, D14, D15, C8, C11, C25, E5, E9–E13)
- `tests/unit/combate/combate_ciclo_parry_test.gd` — ciclo de parry
- `tests/unit/combate/combate_espacio_tuning_test.gd` — espacio de tuning
- `tests/integration/` — empty (CI only targets `tests/unit` per `.github/workflows/tests.yml`)
- `tests/helpers/` — boss_stub, parry_harness, signal_order_spy

---

### Manual Smoke Checks

- [-] Game launches without crash — N/A (no menu/build; `tests/smoke/critical-paths.md` items 1–3 list as *pendiente: no hay menú*; manual Batch 1 returned Unanswered)
- [-] New game starts — N/A (same as above)
- [x] Fórmulas parry-absorción — PASS (via automated standalone, 47/47 checks across formulas/invariants/clamps)
- [x] Ciclo de parry contra stub incl. C12a/C24/C19–C21/C6 — PASS (via automated standalone, 10/10)
- [x] Stub de jefe determinista / disparador Ventana Especial — PASS (via automated standalone C19–C21 + items 4–5 of critical-paths)
- [-] Save / load — N/A (no save system; critical-paths items 8–9 *pendiente*; Batch 3 returned Unanswered)
- [-] Performance — not checked this session (critical-paths items 10–11 *pendiente*; Batch 3 returned Unanswered)

Manual batch responses (verbatim): Batch 1 "Unanswered", Batch 2 "Unanswered", Batch 3 "Unanswered" — no failures selected in any batch.

---

### Missing Test Evidence

Stories that must have test evidence before they can be marked COMPLETE via
`/story-done`:

"All Logic and Integration stories have test coverage." (No stories in scope — no MISSING
entries traceable. Advisory: `tests/integration/` is empty; add integration tests when
cross-system work lands.)

---

### Verdict: PASS WITH WARNINGS

Rules applied: no automated failures (standalone 57/57 PASS), no Batch 1/2 FAIL
reported → not FAIL. gdUnit4 suite NOT RUN unconfirmed + no QA plan + manual batches
unanswered (not explicitly confirmed) → warnings present → PASS WITH WARNINGS, not clean PASS.

Advisory items:
1. Confirm gdUnit4 suite status from local IDE or CI (3 unit files in `tests/unit/combate/`) — resolves the NOT RUN row.
2. No QA plan found — run `/qa-plan sprint` before the next smoke check so coverage can map to stories.
3. Manual core-stability / save / performance paths are not executable yet (no menu, scenes, or save system) — tracked as N/A, not PASS.
4. `tests/integration/` is empty — expected at this stage (Systems Design, 1/21 systems coded), revisit when cross-system work lands.
